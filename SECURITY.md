# 安全说明

## 不得提交的内容

- Android签名证书和私钥
- 证书密码、GitHub Token和API Key
- `.env`及其他包含密钥的配置
- 用户聊天记录、个人资料或测试数据

如果发现敏感信息意外进入Git历史，应立即撤销对应凭据，不能只删除最新文件。

## 更新安全

APP必须同时依赖Android系统的APK签名校验和清单中的SHA-256。SHA-256用于发现下载损坏，Android签名用于确认更新来自同一发布者。

